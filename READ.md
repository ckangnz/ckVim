#CK Vim Config

### Install Vundle
On terminal

    git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

### Install Packages
Open vim and hit

    ,pi

    
### Symlink .vimrc
On terminal

    ln -s ~/.vim/.vimrc .vimrc
